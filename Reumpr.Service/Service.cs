using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.IO;

namespace tetryds.Reumpr.Service
{
    public class Service
    {
        //ConcurrentDictionary<string, Action<Request, Handler>> actionMap = new ConcurrentDictionary<string, Action<Request, Handler>>();

        //public void Add(string url, Action<Request, Handler> action)
        //{
        //    if (!actionMap.TryAdd(url, action))
        //        throw new ArgumentException($"Url '{url}' is already registered", nameof(url));
        //}
    }
}
